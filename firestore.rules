rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /usuarios/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      match /ninos/{ninoId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      match /relojes/{relojId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
